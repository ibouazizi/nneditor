"""Source-format adapters.

Each adapter maps one artifact contract from :mod:`nneditor.ir.capabilities`
into the canonical IR, preserving source-specific data in extension namespaces
rather than discarding it.
"""

"""Graph analysis: layout now, regions and patterns in Phase 2.

Everything in this package operates on IR values and returns plain data.
Architectural rule 5 applies: these are replaceable algorithm implementations
behind small interfaces, never a dependency of the IR itself.
"""
